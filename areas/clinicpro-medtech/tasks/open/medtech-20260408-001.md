---
id: medtech-20260408-001
title: Test ExplanationOfBenefit, Task, and Communication endpoints
project: clinicpro-dashboard
repo: clinicpro-medtech
milestone:
status: open
priority: high
created: 2026-04-08
due:
---

Roles granted by Defne on 6 April. Invoice and ChargeItem tested. These three remain.

1. ExplanationOfBenefit — ACC claim details per patient. Test: `GET /FHIR/ExplanationOfBenefit?patient=Patient/{id}` using UAT patient ZZZ0032 (e92d8ded1459f2d6c5096282382ca67b)
2. Task — clinical action item tracking. Test: `GET /FHIR/Task?patient=Patient/{id}` and `POST /FHIR/Task` write-back
3. Communication — inbox messages. Test: `GET /FHIR/Communication?subject=Patient/{id}` and `POST /FHIR/Communication` write-back

For each endpoint, confirm:
- HTTP 200 with expected Bundle structure
- Response fields match what the billing/action item/inbox modules need
- Write-back (POST) returns 201 with created resource ID

Run via SSH on Lightsail BFF using same auth pattern as Invoice tests.
