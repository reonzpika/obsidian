---
id: saas-20260408-003
title: Visual QA referral-images landing page redesign
project: referral-images
repo: clinicpro-saas
milestone: "Referral Images landing page"
status: open
priority: medium
created: 2026-04-08
due: 2026-04-11
---

Manual browser smoke test of the redesigned landing page at `/referral-images` after the Native palette / Inter / 11-section rewrite.

1. `cd C:/Users/reonz/Cursor/clinicpro-saas && npm run dev`, open http://localhost:3000/referral-images logged out
2. Walk all 11 sections at 375 px (mobile), 768 px (tablet), 1280 px (desktop) — confirm no layout breaks, especially the merged Scope + Fit 4-card grid and the dark security section
3. Verify hero stagger animation fires once on load and the amber underline accent draws in
4. Scroll past 24 px — confirm header transitions to glass blur
5. Hover the green CTA — confirm green glow (`rgba(21,128,61,0.18)`), not the legacy blue
6. Open + close the FAQ accordion (10 items)
7. Click "Start free trial" → confirm redirect to `/auth/register?redirect_url=...` (auth flow unchanged)
8. Spot-check WCAG AA contrast on white text over `nz-green-700` (final CTA banner) and `slate-500` over `slate-50`
9. Logged-in path: confirm `ensure-links` POST still fires and routes to `/referral-images/capture?u=...` on mobile or `/referral-images/desktop?u=...` on desktop
10. Cosmetic cleanup pass: ESLint `--fix` left some `<br />` tags and `{' '}` segments awkwardly broken across lines — tidy these manually if obvious during review
