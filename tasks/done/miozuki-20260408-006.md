---
id: miozuki-20260408-006
title: Sprint 2 — Afterpay, product card tags, Complete the Look, founder pull-quote, video gallery
project: miozuki
repo: miozuki-web
sprint: 2026-04-miozuki-sprint-2
status: done
priority: medium
created: 2026-04-08
due: 2026-04-15
---

Plan reference: `~/.claude/plans/replicated-soaring-shell.md` §2.

Cut new branch off `master`: `feat/sprint-2-metafields-xsell`.

## Items

### §2.1 Afterpay badge near PDP price
- **Blocker**: confirm Miozuki is approved Afterpay merchant in Shopify Payments first.
- New `components/product-price.tsx` (client) — receives `amount` + `currencyCode`, renders price + "or 4 interest-free payments of $X with Afterpay" using `Intl.NumberFormat('en-NZ', ...)`.
- New asset: `public/payment-logos/afterpay.svg`.
- Mount in `app/products/[handle]/page.tsx:84-98` price block.

### §2.2 Product card material/stone tags + Playfair title
- `components/product-card.tsx` — filter `product.tags` for whitelist (`pearl`, `moissanite`, `silver`, `gold-fill`), render small `<span>` row above title.
- Title className: `font-serif text-sm` (Playfair). Price stays DM Sans.
- **Owner dependency**: each Shopify product needs at least one whitelisted tag.

### §2.3 "Complete the Look" cross-sell on PDP
- New `components/complete-the-look.tsx` (server) — 3-up grid.
- Curation source: Shopify metafield `custom.complete_the_look` (type `list.product_reference`). Add to `lib/shopify/queries.ts` `GET_PRODUCT_BY_HANDLE`.
- Fallback: query same collection, pick first 3 sibling products. Avoids empty state at launch.
- Mount in `app/products/[handle]/page.tsx` between description and reviews suspense.

### §2.4 Founder pull-quote band on homepage
- 2-sentence pull-quote + circular founder portrait + link to `/pages/our-founder`.
- Mount in `app/page.tsx` between Best Sellers and Differentiator strip.
- **Dependency**: founder headshot from photography track. Optionally an AI hands-only stand-in via Sprint 1.5 tooling (face out of frame; AI portraits are HIGH risk per research §5/§6).

### §2.5 Sparkle video slot in product gallery
- `components/product-gallery.tsx` — extend prop type to accept video media alongside images.
- `lib/shopify/queries.ts` — add `media(first: 10) { edges { node { mediaContentType, previewImage { url }, ... on Video { sources { url, mimeType } } } } }` to `GET_PRODUCT_BY_HANDLE`.
- Render `<video autoplay muted loop playsinline>` for `mediaContentType === 'VIDEO'`.
- **Guarded**: if no videos exist, gallery behaves as before. Ships safely without assets.
- Video assets land on photography track. AI-generated sparkle videos out of scope (high AI-tell risk).

## Pre-merge

1. Run `npm run build`, `npm run lint`, `npx tsc --noEmit`.
2. Confirm Afterpay merchant status with owner before shipping §2.1.
3. Verify at least one Shopify product has a whitelisted tag for §2.2.
