---
id: miozuki-20260408-002
title: PDP trust strip — returns, warranty, stone data below Add to Cart
project: miozuki
repo: miozuki-web
sprint: 2026-04-miozuki-sprint-2
status: done
priority: high
created: 2026-04-08
due: 2026-04-13
---

**Update 2026-04-08 (PM)**: shipped to `master` in commit `02252c4` and pushed to `origin/master`. Vercel will auto-deploy. Owner sign-off on the live preview tracked separately in `miozuki-20260408-007`.

**Update 2026-04-08**: implemented on `feat/sprint-1-quick-wins`, awaiting merge + deploy.
- New `components/pdp-trust-strip.tsx` (4-cell server component) mounted in `app/products/[handle]/page.tsx` directly under `<AddToCart>`. Cells: free shipping $150+, 30-day returns, lifetime warranty, secure checkout.
- Materials accordion in `app/products/[handle]/page.tsx` always renders now and always includes a hard-coded stone data block (Mohs 9.25, RI 2.65, DEF colour, VVS clarity, AGS/IGI graded). Metafield value (when present) renders above the fallback.
- Shipping copy updated to free $150+ in same file (line ~152).
- "GRA" not mentioned anywhere — compliance per audit.


Add a 4-line trust strip below the Add to Cart button on every PDP (`app/products/[handle]/page.tsx`).

Lines to include:
1. Free returns within 30 days (unworn, original packaging)
2. Lifetime warranty on every piece
3. Mohs 9.25 hardness — Moissanite stone data (RI 2.65, AGS/IGI certified)
4. Ships from Auckland, NZ via NZ Post with tracking

Also add stone data (Mohs, RI, certification) to the Materials accordion on PDPs.

Notes:
- Style: small icons + text, cream background, charcoal/50 text — consistent with differentiator strip on homepage
- Do not add a review widget placeholder (zero-star increases abandonment)
- Returns copy must match Shopify policy page (30-day window)
