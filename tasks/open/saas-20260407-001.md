---
id: saas-20260407-001
title: Fix React setState warning on referral-images success page
project: referral-images
repo: clinicpro-saas
sprint: 2026-04-sprint-1
status: open
priority: low
created: 2026-04-07
due: 2026-04-14
---

`app/(clinical)/referral-images/success/page.tsx:25` — `router.push('/referral-images')` is called inside a `setInterval` callback inside `useEffect`, triggering a React warning: "Cannot update a component while rendering a different component". The redirect works but the pattern is wrong.

Fix: use a `useRef` for the interval and call `router.push` after `clearInterval` in the same tick, or restructure the countdown with a `useEffect` cleanup so the navigation happens cleanly.
