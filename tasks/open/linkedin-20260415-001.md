---
id: linkedin-20260415-001
title: Debug and update LinkedIn scout agent
project: linkedin-agent
repo: linkedin
sprint:
status: in-progress
priority: high
created: 2026-04-15
due: 2026-04-22
---

Scout agent stopped working. Last successful run was 2026-03-25.

## Progress (2026-04-15 session)

Three of four candidate root causes addressed by the wider audit + Claude-Code-as-LLM refactor; the remaining one needs live verification.

| Candidate | Status | Action taken |
|---|---|---|
| Import error from deprecated `graph.state` | **Resolved** | `graph/` directory deleted; `agents/scout.py` no longer imports `LinkedInContext` (uses plain `dict`). |
| Session cookies expired | **Verified** | Confirmed: `playwright-cli state-load auth/linkedin_session.json` redirected to `/uas/login`. **User must run `python scripts/login.py` before next attempt.** New `scripts/auth_preflight.py` catches this proactively. |
| Feed DOM selectors broken | **Mitigated, not fixed** | New `linkedin-selector-repair` skill + `@playwright/cli` v0.1.8 installed. Repair loop is now ~5 minutes when a selector breaks. Actual selector status unknown until live run. |
| User-agent fingerprinting | **Not addressed** | Still Chrome/120 macOS UA. Audit recommended platform-matching UA but not yet shipped. |

## Next session

1. Run `python scripts/login.py` to refresh session.
2. Run `python scripts/auth_preflight.py` — expect exit 0.
3. Run `python scripts/scout.py --session-dir outputs/2026-03-25_inbox-magic-founder-meeting` and read the full output.
4. If selectors broke during the 3-week gap, invoke `linkedin-selector-repair` skill to find the new selectors live via `playwright-cli`.
5. Once scout works, the cutover is fully verified end-to-end.

Tracked separately as `linkedin-20260415-002` (verify post-refactor + cron auth_preflight).

## Key files (unchanged)

- `agents/scout.py` — Playwright-only, no LLM. `max_posts` dropped from 60 → 30 in this session.
- `tools/browser.py` — `scrape_personal_feed`, `FEED_ACTIVITY_POST_SELECTOR` at line 20.
- `config/playwright_settings.py` — Auth flow, now with `li_at` cookie expiry pre-check.
