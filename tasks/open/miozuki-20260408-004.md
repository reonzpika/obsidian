---
id: miozuki-20260408-004
title: Sprint 1 quick wins — engraving, colour, nav, contact form, OG metadata
project: miozuki
repo: miozuki-web
sprint: 2026-04-miozuki-sprint-2
status: in-progress
priority: high
created: 2026-04-08
due: 2026-04-10
---

Umbrella task for the brand-cohesion items shipped on `feat/sprint-1-quick-wins` that are NOT covered by 002, 003, or 007.

Plan reference: `~/.claude/plans/replicated-soaring-shell.md` §1.

## Implemented (code complete, awaiting merge to master)

- **§1.1** PDP shipping copy — free over $150 NZD (`app/products/[handle]/page.tsx`).
- **§1.2** PDP trust strip — see 002.
- **§1.3** Materials accordion stone data fallback — see 002.
- **§1.4** Engraving char limit 4→12 (`components/add-to-cart.tsx` lines 122-127, input width w-28→w-52).
- **§1.5** Brand colour tokens — `--accent` L 0.33→0.45, `--accent-hover` 0.27→0.38, `--color-burgundy` matched, `--surface` 0.93→0.90 (`app/globals.css`).
- **§1.6** "Learn About Moissanite" removed from Rings dropdown + mobile nav; moved to footer Help column (`components/header.tsx`, `components/footer.tsx`).
- **§1.7** Footer NZ trust signals — see 003.
- **§1.8** Contact form wired to Klaviyo Events API (`app/api/contact/route.ts`, `components/contact-form.tsx`, `app/pages/contact/page.tsx`). Reuses `KLAVIYO_PRIVATE_KEY`. Owner sees submissions in Klaviyo dashboard as "Contact form submission" metric events.
- **§1.9** OpenGraph + Twitter + metadataBase metadata in `app/layout.tsx`. **TODO**: `images: ['/og-image.jpg']` to be added once OG asset is generated via miozuki-20260408-005.
- **§1.11** Hero headline — kept current "Fine jewellery / inspired by the moon" (recommended for cold-traffic clarity). No code change.

## Deferred (blocked on §5 image tooling)

- **§1.9 OG image asset** — needs Nano Banana Pro tooling (see 005).
- **§1.10 Replace AI-generated "Accessible Luxury" image** — needs Nano Banana Pro tooling (see 005).

## Verification before merge

1. `npm run build` — ✅ passes (81 static pages, /api/contact registered).
2. `npm run lint` — ✅ passes.
3. `npx tsc --noEmit` — ✅ passes.
4. Local smoke walk: homepage → 2 PDPs → contact form → footer.
5. Klaviyo dashboard check after contact form test submit.

## Pre-merge requirements

- Confirm `KLAVIYO_PRIVATE_KEY` is set in Vercel project env vars (already required for /api/subscribe).
- Owner walks the local preview, signs off on engraving width + new burgundy intensity.
